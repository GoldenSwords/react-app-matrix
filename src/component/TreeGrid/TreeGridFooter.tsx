import * as React from 'react';
import classnames from 'classnames';

import './TreeGridFooter.scss';

interface IProps {
}

const TreeGridFooter: React.FC<IProps> = (props: IProps) => {
  return (
    <div className="footer">footer</div>
  );
};

export default TreeGridFooter;
